#include <iostream> 
#include <math.h> 
#include <iomanip> 
 
using namespace std; 
 
int main()  
{ 
 double y, a, x; 
 a = 0.83; 
 x = 1.2; 
 
 cout << "seach y " << endl; 
 y = (sqrt(a * x * x + pow(log10(2 * pow(sin(x), 2)), 1/ 3.0))) - (atan2(a - x, 4.5)); 
 
 cout << "rezelut = " << y << endl; 
 return 0; 
 
}
